import React from 'react';
import ThemeControl from '../theme-control';

const Sidebar = () => {
    return (
        <div>
            <h2>Sidebar</h2>
            <ThemeControl />
        </div>
    );
};

export default Sidebar;
